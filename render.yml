services:
  - type: web
    name: imdb-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 app.py
    envVars:
      - key: PORT
        value: 10000  # Render overrides this automatically
